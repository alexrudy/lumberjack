[loggers]
keys = root

[handlers]
keys = stream

[logger_root]
handlers = stream

[handler_stream]
class = lumberjack.streams.ColorStreamHandler
fmt = "%(clevelname)s: %(message)s [%(name)s] [%(asctime)s]"
level = NOTSET
